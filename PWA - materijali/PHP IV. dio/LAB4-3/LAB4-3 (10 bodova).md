# LAB4-3 (10 bodova)

## Zadatak

Napravite formu koja sadrži polja za unos imena, prezimena, grada i poštanskog broja. 
Napravite bazu s tablicom koja se sastoji od slijedećih kolona; id (int, autoincrement), ime (varchar), prezime (varchar), grad (varchar), postanski_broj (int).

Napišite php skriptu koja u bazu upisuje vrijednosti unesene u formu. Unos trebate zaštitit na SQL injection napade koristeći prepared statement.